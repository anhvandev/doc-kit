# Thư viện bên thứ ba nhúng trong HTML sinh ra

| File | Dự án | Phiên bản | Giấy phép | Nguồn |
|---|---|---|---|---|
| `mermaid.min.js` | [mermaid](https://github.com/mermaid-js/mermaid) | 11.17.2 | MIT (`MERMAID-LICENSE.txt`) | `https://cdn.jsdelivr.net/npm/mermaid@11.17.2/dist/mermaid.min.js` |

Bản IIFE, gán `globalThis.mermaid`, không dùng `import`, nên chạy từ `file://`. Chỉ nhúng vào trang có khối `mermaid`. Nâng phiên bản: tải lại đúng một file, cập nhật bảng này và `MERMAID-LICENSE.txt`.
